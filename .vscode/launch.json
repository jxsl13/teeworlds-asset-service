{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run asset-service (local docker DB)",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/docker/dev.env"
    }
  ]
}
